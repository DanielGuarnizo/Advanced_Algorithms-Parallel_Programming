{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "(lldb) Build and Debug Active File",
            "type": "cppdbg",
            "request": "launch",
            "program": "${fileDirname}/${fileBasenameNoExtension}",
            "args": ["7", "8", "2", "4", "9", "3", "6"],
            "stopAtEntry": false,
            "cwd": "${fileDirname}",
            "environment": [],
            "externalConsole": false,
            "MIMode": "lldb", // CRITICAL: Use lldb on macOS
            "preLaunchTask": "C/C++: clang++ build active file"
        }
    ]
}